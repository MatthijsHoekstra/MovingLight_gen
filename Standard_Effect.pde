class Effect {

  int direction, duration, distance;
  
  int x, y;
  
  Effect(int distance_){
    distance = getLength(distance_);
  }
  
  boolean finished() {
    if (direction == LEFT_RIGHT) {
      //if (
    }
    if (direction == RIGHT_LEFT) {
    }
    return false;
  }
}